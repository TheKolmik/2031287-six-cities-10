export type Points = {
  id: number,
  title: string,
  lat: number,
  lng: number,
}

export type City = {
  title: string,
  lat: number,
  lng: number,
  zoom: number
}

export type NearPlaces = {
  id: number,
  title: string,
  lat: number,
  lng: number,
}
